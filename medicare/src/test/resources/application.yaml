spring:
  application:
    name: medicare

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

  mongodb:
    uri: mongodb://admin:admin123@localhost:27017/medicare?authSource=admin

jwt:
  secret: "dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIG1lZGljYXJlIGFwaSBqd3QgdG9rZW5z"
  expiration: 86400000
